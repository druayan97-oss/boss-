# Boss直评 — 产品共识与 MVP 规格（讨论沉淀）

**文档日期**：2026-04-05  
**状态**：共识记录（供后续实现与评审使用）  
**说明**：本文档汇总当前已对齐的产品决策；未在对话中拍板的事项单独列为「待决」。

---

## 1. 背景与要解决的问题

- 求职者在招聘平台（如 Boss 直聘）侧常见痛点：**已读不回**、筛选规则不透明、关系上偏被动。
- 产品设想 **Boss直评**：由求职者对招聘方（以 HR 为粒度）进行**公开打榜/提名**，形成可参考的社区榜单；后续还可扩展「模拟招呼—猜回复」等玩法。

---

## 2. 已对齐的产品方向

### 2.1 MVP 优先级

| 决策 | 内容 |
|------|------|
| **MVP 验证重点** | **A：打榜/点名体系**（先验证传播与需求强度） |
| **暂不优先** | **B：模拟招呼游戏**（选择 Boss、接收招呼流、猜回复准确率等）——可在 MVP 之后再排期 |

### 2.2 「真实」与展示策略

| 决策 | 内容 |
|------|------|
| **标识方式** | **不弱化**：榜单侧以**可指向真实场景**的信息为主（如 HR 姓名、公司、招聘岗位等），不做「仅公司/岗位聚合弱化个人」的替代方案（至少当前共识如此） |

### 2.3 内容与审核策略

| 决策 | 内容 |
|------|------|
| **凭证** | **不要求用户上传截图/附件**；避免额外存储与基于上传材料的审核链路 |
| **AI** | **不依赖 AI** 做内容审核或凭证分析（成本与复杂度考量） |
| **治理** | **事后举报**为主：通过举报与运营/规则流程处置争议与滥用（具体阈值与自动化程度待实现阶段细化） |

### 2.4 账号与身份

| 决策 | 内容 |
|------|------|
| **登录方式** | **邮箱登录**；参与提名/打榜等写入类操作需登录（与「非完全匿名」一致，便于频控与追责） |
| **邮箱验证（已定稿，分两阶段）** | **长期**：产品需具备**邮件验证链路**（如发送验证链接、记录邮箱是否已验证），作为正式规则写进数据模型与权限判断。**冷启动**：当平台**累计注册用户数不足 200** 时，**不强制**用户完成「点击邮件验证链接」即可**正常注册并执行写入类操作**（提名/打榜等）。**扩容后**：当**累计注册用户数达到或超过 200**，或运营通过 **功能开关** **提前**开启强制验证时，**强制**未完成邮箱验证的账号**不得**执行写入类操作（或采用等价策略：仅只读浏览），直至用户完成验证。计数口径：**已完成注册流程的独立账号数**（实现时以数据库唯一用户表为准）。 |

### 2.5 提名去重（同用户—同 HR）

| 决策 | 内容 |
|------|------|
| **一人一票（记录级）** | 同一登录用户对**同一个 HR 实体**在系统中**至多存在一条有效提名记录**；**榜单计数上该用户对该 HR 至多贡献 1 票**。 |
| **变更方式** | 用户可通过 **编辑** 该条记录更新内容；或通过 **撤销/删除** 该条记录后 **重新提交**（撤销后票数相应收回，直至再次提交）。 |

### 2.6 公司与 HR 名称规范化（无 LLM）

| 维度 | 策略 |
|------|------|
| **公司（高频）** | 维护一份**人工/运营维护的高频公司名单**（canonical 名称 + 可选别名）。用户输入先经**轻量规范化**（见下文化洗「通用步骤」），再与名单做**模糊匹配**（如编辑距离、Jaro–Winkler 等，**阈值与候选条数上限**在实现时调参）；匹配成功则**挂靠 canonical id**，榜单与聚合以 canonical 为准。 |
| **公司（低频）** | 未命中名单的**小公司/低曝光名**：**不做**强归一，原样保留规范化后的字符串即可；**误匹配成本可接受**（产品共识）。后续若某名频次升高，可再**加入名单**。 |
| **HR 姓名** | **不调用 LLM**。采用**可审计的规则管道**生成用于匹配的 **`hr_key`（内部键）**；**展示名**可保留用户原始输入或「清洗后展示名」（实现二选一，需防止过度改写引发争议）。**同用户—同 HR** 的去重（§2.5）在逻辑上基于 **`canonical_company`（或低频下的规范化公司串） + `hr_key`**。 |

**HR 清洗管道——建议实现的规则层（自上而下，均可单测；命中规则应可记录便于排错）：**

1. **Unicode 规范化**：统一为 **NFKC**，消除兼容字形、全角/半角混用带来的假差异。  
2. **不可见与空白**：去除零宽字符；全角空格等映射为普通空格；**trim** 与**连续空白折叠**为单空格。  
3. **剥离职位/身份噪音（词典 + 边界）**：从**后缀/前缀**移除高频非姓名片段，例如：`HR`、`人事`、`招聘`、`猎头`、`经理`、`主管`、`负责人`、`老师` 等；**须避免**误删过短真名（规则优先匹配**完整词/分隔符边界**，慎用单字规则）。  
4. **括号与分隔符段**：对形如 `张三（人事）`、`李四｜招聘`、`王五 - HR` 等，按模式**去掉括号内职位段**或**竖线/短横后的职位段**；保留主体姓名部分。  
5. **英文片段**：对拉丁字母部分做 **Unicode case fold** 生成匹配键；**展示**可保留用户原大小写。  
6. **重复与异常**：连续重复字符（如 `张张`）可标记为**待二次确认**或按策略折叠为单字（可选，需防误伤）；对**过长/过短/纯符号**输入可拒绝或要求用户修正。  
7. **可选增强（仍无 LLM）**：维护小型**同形字/易混字映射表**（用户手写易混字，非模型生成）；对**常见拼音输入法近音错字**可用**预置纠错表**（按批添加，避免泛化误纠）。  

**说明**：以上管道解决的是「**同一真人在不同输入法下的写法收敛**」；**「小张」与「张明」是否为同一人**等语义问题**不在 MVP 自动解决**，可留待举报/运营或远期人工合并。

---

## 3. MVP 功能范围（建议实现边界）

### 3.1 建议纳入 MVP（与当前共识一致）

- **用户体系**：邮箱注册/登录、会话管理；**实现上预留**发信与 `email_verified`（或同类字段）。**强制验证**在**注册用户不足 200** 时关闭；**达到 200** 或运营开关提前开启后，未验证邮箱不可写入（与 §2.4 一致）。
- **提名/记录**：用户可提交与某次求职经历相关的 **HR 可识别信息 + 公司 + 岗位**（字段细则可在技术设计时定稿）。**同一用户对同一 HR 仅一条有效记录**，可编辑或删后重发（与 §2.5 一致）。**公司名**对高频主体走 **§2.6 名单 + 模糊匹配**；**HR 名**走 **§2.6 规则清洗管道** 生成内部匹配键（无 LLM）。
- **榜单**（可分两期，但均在「打榜」大方向内）：
  - **已读不回相关计数/排行**（具体是「人次」还是「独立 HR 条目聚合」待技术/数据模型决）。
  - **公司维度榜单**（例如按公司聚合负面/正面提名量——与「不弱化个人」并存时，需明确是否同时展示个人榜与公司榜）。
- **正向榜**：**Nice HR 提名**（与负面类标签并列存在，对话中已提出，建议 MVP 至少保留入口或二期紧邻上线，避免产品只剩「泄愤」单一叙事）。
- **举报**：对单条记录或实体（HR/公司维度）发起举报；后台或简易运营队列处理（实现深度待定）。

### 3.2 明确后置（当前轮对话中未选为 MVP 核心）

- **「听不懂人话 HR」榜**、**求职最难岗位榜**：作为 **标签/分类扩展**，建议在个人榜与公司榜跑通后再加，避免规则爆炸。
- **模拟招呼 / 猜 Boss 回复准确率（B）**：整模块 **非 MVP**。

---

## 4. 风险与实现侧隐含要求（开发需知情）

以下不构成法律意见，仅作产品/工程风险提示：

- **可识别个人信息 + 负面评价** 易引发名誉与合规争议；**无上传凭证** 会提高 **误伤、造谣、商业抹黑** 概率，**举报与处置 SLA** 会成为产品可信度核心。
- **防刷**：在「无附件、无 AI」前提下，更依赖 **账号维度频控**、**重复条目合并**、**同 HR 多来源合并策略**、必要时 **设备/IP 辅助风控**（具体方案待技术选型）。
- **与第三方平台关系**：产品不假设与 Boss 直聘等存在官方合作；对外表述需避免暗示「官方数据」或「实时同步对方行为」。

---

## 5. 待决事项（后续一次对话或 PRD 补全）

| 编号 | 议题 | 说明 |
|------|------|------|
| T1 | **跨用户实体碰撞** | §2.6 已覆盖「公司挂靠名单 + HR 规则键」；仍可能存在**同公司同名不同人**、**昵称与实名**等，是否 MVP 只做展示备注或完全依赖举报/运营后置处理。 |
| T2 | **举报成立后的动作** | 隐藏、降权、删除、标注「争议中」等。 |
| T3 | **榜单排序维度** | 仅计数、时间衰减、举报加权惩罚等。 |

---

## 6. 建议的下一步（工程）

1. 将本文档作为 **v0 共识基线**，在仓库中随迭代更新版本与变更记录。  
2. 对 **T1～T3** 做一轮短评审后，可再产出 **API/数据模型草图** 与 **页面流程（线框）**。  
3. 若进入编码阶段，可按 superpowers 流程另写 **implementation plan**（与本文档区分：本文档偏「产品共识」，计划偏「任务拆分与里程碑」）。

---

## 7. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-04-05 | 初稿：根据多轮对话沉淀 MVP、内容策略、登录方式及待决项 |
| 2026-04-05 | 邮箱验证策略定稿：长期具备验证能力；冷启动不强制验证链接，规模达阈值或开关开启后强制验证后方可写入 |
| 2026-04-05 | 项目根目录初始化 Git 仓库并纳入文档与工作日志 |
| 2026-04-05 | 强制邮箱验证阈值定为累计注册用户 **200**；保留运营功能开关可提前开启 |
| 2026-04-05 | 提名规则：同一用户对同一 HR **仅一条有效记录**，可编辑或删后重发；该用户对应该 HR **至多 1 票** |
| 2026-04-05 | §2.6：高频**公司名单** + **模糊匹配** canonical；低频公司弱处理；**HR 名**规则清洗管道（无 LLM）及可选增强说明 |
